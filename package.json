{
    "name": "wayra-turismo",
    "version": "1.0.0",
    "engines": {
        "node": "24.x"
    },
    "scripts": {
        "build": "mkdir -p dist && cp -rp . dist/ && rm -rf dist/api dist/netlify dist/dist dist/node_modules dist/package.json dist/vercel.json dist/laravel-project"
    },
    "dependencies": {}
}